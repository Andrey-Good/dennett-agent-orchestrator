
# Tests

- `contracts/`: port/adapter parity and protocol compatibility.
- `integration/`: real processes with disposable stores/providers.
- `e2e/`: desktop/mobile user journeys.
- `simulation/`: deterministic clocks, partitions, duplicate/reordered events and crashes.
- `scenarios/`: compact declarative failure scenarios; complex logic stays in Rust/Python test code.
