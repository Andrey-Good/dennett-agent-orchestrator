
# Scenario Test Instructions

Scenarios describe topology, commands, injected faults and observable expectations. They are not workflow definitions and must not contain product logic.

Every scenario needs:
- deterministic IDs/seed;
- explicit authoritative state;
- fault timing;
- observable result;
- recovery expectation;
- no real secrets or personal data.
