# Working capture notes

This directory is intentionally small. Keep one active capture per milestone, consolidate it into `../evidence/` after acceptance, then remove facts that are duplicated elsewhere.

Do not store secrets, raw private conversations, full reasoning traces, local absolute paths or unredacted screenshots here.
