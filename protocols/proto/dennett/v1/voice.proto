
syntax = "proto3";
package dennett.v1;

message CommittedVoiceTurn { string session_id = 1; uint64 turn_revision = 2; string speaker = 3; string text = 4; }
message SpokenOutputCommit { string session_id = 1; uint64 turn_revision = 2; string actually_played_text = 3; bool interrupted = 4; }
