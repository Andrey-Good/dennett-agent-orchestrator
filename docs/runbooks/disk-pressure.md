# Disk Pressure

Reclaim in order: disposable caches → generated previews → rebuildable indexes → expired ring buffers → cold media according to policy. Never silently delete canonical events, the only copy of an artifact, unsynced project changes or effect receipts. At critical pressure pause new raw capture before losing authoritative data.
